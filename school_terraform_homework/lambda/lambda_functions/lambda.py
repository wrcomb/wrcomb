import boto3

ec2 = boto3.client('ec2', region_name='')

# collect all instances with `test` tag

# stop function
def stop_instances(event, context):
    :

# start function
def start_instances(event, context):
    :
